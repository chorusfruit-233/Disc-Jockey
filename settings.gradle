pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven { url = 'https://maven.fabricmc.net/' }
    }

    plugins {
        id 'net.fabricmc.fabric-loom' version "${loom_version}"
    }
}
